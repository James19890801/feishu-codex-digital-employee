# 安全策略

## 支持范围

安全修复只针对当前主分支和最新内部部署版本。James 运行在真实企业会话中，身份、权限、隐私和投递错误都按安全问题处理。

## 私密报告

请通过阿里内部私密渠道联系唯一维护者阿充（James Feng）。不要在公开 Issue、Merge Request、评论或截图中提交：

- Token、Cookie、验证码、设备私钥、签名密钥或恢复材料。
- OpenDingTalkId、工号、手机号、DWS Profile/Channel 等个人标识。
- `config.local.json`、Keychain 内容、SQLite 数据库、日志和备份。
- 未脱敏的聊天、会议、文档、代码和 1A 业务数据。

报告应包含最小复现步骤、影响范围、预期与实际行为、时间点，以及已经脱敏的错误信息。

## 核心边界

- 钉钉只允许独立 DWS `event-stream`，Wukong 和悟空路径被硬拒绝。
- Dashboard 只绑定 `127.0.0.1`；不得改为无认证的局域网或公网监听。
- 高风险操作必须经过 Owner 校验和明确确认。
- 黑名单自动通信不得被模型输出、重试或通知队列绕过。
- 外部写入结果不确定时失败关闭，不自动重放。
- 发行和部署不得包含个人配置与认证材料。

## 响应原则

确认问题后，维护者会先限制影响范围，再补充可复现测试、修复、完整回归和部署说明。没有验证证据前，不宣称问题已经修复。
