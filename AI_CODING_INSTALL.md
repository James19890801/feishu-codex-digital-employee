# macOS AI Coding 一键安装

解压发行 ZIP 后，在 AI Coding 工具的终端中进入解压目录，只运行这一条命令：

```zsh
zsh ./install.command
```

安装器会校验文件完整性，把程序安装到当前 macOS 用户目录，并保留升级前的本地配置、Persona、Bible 与数据。

新安装默认关闭飞书、钉钉、企业微信、个人微信、1A 和其他外部写入能力，也不会自动回复任何人。安装结束后，请在本机 Dashboard 中填写操作者身份，并只启用你明确授权的通道。钉钉 DWS、Codex 或其他 AI Coding 运行时如需 OAuth/登录，必须由使用者本人完成；安装包不包含任何账号、令牌、聊天、文档、记忆或黑名单。

运行服务时，本机文件只在配置允许的具名范围内读取；任何外部消息发送、需求写入和自动通知仍受通道开关、授权范围与人工确认规则控制。
