# 云端规则同步与主备切换进度（2026-09-13）

## 已上线、可验证

- 微信 GeWe 固定入口已在阿里云香港 `wxrelay.e2eskill.cn`；HTTPS 健康检查正常。旧入口保留回退，不因本阶段工作改动其他网站。
- 本机每天 03:30 运行 `com.local.aipro-cloud-parity-sync`，从生产 AIPRO 目录按白名单读取人设、BIBLE、AGENTS、入出站配置、授权会话、屏蔽名单、真人接管和限流/去重/群聊连续性状态，生成确定性摘要。密钥取自 Keychain，服务端用 AES-256-GCM 加密版本化存储；不复制整个数据库、原始入站队列或本机凭据。
- 手工同步已成功，阿里云加密规则快照达到第 6 版。每日任务的最近一次手工 kickstart 退出码为 0，并报告规则摘要吻合。
- 同一每日任务核对指定 Qoder Cloud Agent 的系统人设。Agent 已更新到第 2 版，内置工具列表为空；用指定 Environment 的无私信会话得到预期 smoke 回答。Qoder 只应产出候选回复，不拥有发送权限。
- 主备 generation、90 秒失联阈值、旧 generation 隔离、云端 drain、一次性认领、出站意图/回执去重已有持久化代码和单元测试，但生产未启用。
- 聚焦测试 47/47 通过。全项目 `npm test` 在独立 worktree 中因缺少私有 `config.local.json` 未跑完，不应表述为全量通过。

## 尚未达成，自动接管保持关闭

- 没有云端常驻应用消费微信/钉钉入站并调用 Qoder 后发送原会话回复；没有生产级主备心跳接线和统一两渠道出站回执。
- 钉钉在阿里云的独立 DWS 登录、常驻入站及发送实测未完成。
- 真人接管/拒绝规则目前由每日全量快照同步，运行中变化可能滞后；需要即时关键状态同步及发送前新鲜度检查。全量快照包含连续性状态，但不能代替实时共享账本。
- 本地关机、云端真实微信/钉钉回复、重复回调、Qoder 故障与本地恢复回切的端到端演练未完成。未取得微信历史补拉和钉钉全会话搜索权益，断线区间历史覆盖仍是“未查全”。

## 上线门槛

必须先完成独立钉钉认证、同源策略判断、关键状态即时同步、每条消息发送前 generation/黑白名单/真人接管复核、双渠道真实回执，以及停本地服务后的受控切换和回切测试。否则宁可保持本地为唯一执行端，也不自动云端发信。
