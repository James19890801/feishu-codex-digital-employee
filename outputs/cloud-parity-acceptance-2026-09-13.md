# 本地优先 / 阿里云云端接管验收台账（2026-09-13，进行中）

## 微信优先决策与最新开发进度

用户已确认微信可独立完成云端验收和上线，钉钉授权不再是微信的前置门槛。微信专属就绪检查、专属发送适配器和禁止认领钉钉事件已落地并通过聚焦测试；接管判断现从服务器持久化的主进程心跳、策略版本和游标读取，不接受调用方自报心跳。这些是**代码进度**，不是生产启用回执。当前仍缺真实微信云端发送凭据及供应商回执测试、主进程心跳接线、统一发送权、真实停机/回切验收，自动接管保持关闭。

## 截止本次检查

| 环节 | 微信 | 钉钉 | 证据与结论 |
| --- | --- | --- | --- |
| 固定云端入站 | 已上线 | 未上线 | 微信 `wxrelay.e2eskill.cn/healthz` 返回 200；这仅证明中继健康，不证明历史上游消息已全覆盖。 |
| 云端独立渠道认证 | 未配置发送凭据 | 未登录 | 阿里云独立 DWS v1.0.55 已安装，隔离服务账户 `auth status` 为未登录；等待设备 OAuth。 |
| Qoder 云端推理 | 共享 | 共享 | 指定 Agent v2 / Environment 的无私信真实 Session 返回预期 `AIPRO_CLOUD_OK`；SSE 长连接完成事件处理经复测。 |
| 人设、规则与状态 | 每日加密快照 | 每日加密快照 | 已有白名单摘要同步，但关键状态即时变更与发送前一致性闸门未上线；不构成 100% 同步。 |
| 云端收发与回执 | 未实测 | 未实测 | 出站适配器、一次性意图和回执校验仅通过模拟测试；供应商真实返回字段未完成两渠道验证。 |
| 本地停机接管 | 未执行 | 未执行 | 主进程 15 秒心跳、策略/关键状态游标及生产控制密钥尚未接线；不能安全停本地。 |
| 云端回切本地 | 未执行 | 未执行 | 三次健康心跳、云端排空及代数递增已有单元测试，尚无生产演练。 |

## 安全门槛

目前不得自动启用云端回复。微信先行时无需等待独立钉钉授权，但必须接入本地主进程心跳、微信统一出站闸门，并证明所有必须回复触发（原生/文字 @、小詹提及、引用追问）、黑白名单、真人接管和群聊 @ 语义一致。微信真实消息回执测试通过后才受控停本地、接管、回切；钉钉继续本地运行，单独验收。

微信上游 GeWe 缺历史补拉；钉钉全会话搜索缺权益。任何断线窗口必须标记“未查全”，不能用队列清零推断无漏消息。

## 本次测试

- 聚焦自动化：68/68 通过。全项目私有配置缺席的独立工作树未能完成 `npm test`，不冒称全量通过。
- 本地生产：`com.local.aipro-main` 和微信 edge relay 均运行；云端 `/healthz` 返回 200。
- 真实 Qoder：无私信 smoke 成功；不等于 IM 端到端成功。
- 钉钉：服务端设备授权尚待账号本人确认，未发送测试消息。
