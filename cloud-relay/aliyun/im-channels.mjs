// Development wrapper.  deploy.sh stages the production implementation and
// its two local dependencies at this exact path on the relay host.
export { normalizeGeWeWebhook } from '../../src/im-channels.mjs';
