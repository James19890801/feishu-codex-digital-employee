# 阿充数字人提交验收单

## 代码与配置

- [x] `config.local.json` 仅在本机生效，未提交凭据。
- [x] 企业身份统一为“阿充（James）”、AI 产品经理。
- [x] 运行时为 Codex CLI，不是悟空，不是 Multica。
- [x] 飞书已关闭，不参与健康判定或消息处理。
- [x] 外部写操作保留授权、审计、去重和失败边界。

## 钉钉主链路

- [x] 仅使用本机原生 DWS `event-stream` 通道，未接入悟空轮询。
- [x] DWS 真人身份已认证，个人事件流已连接。
- [x] 每次普通对话回复前，实时读取当前会话最新 30 条记录。
- [x] 回复上下文包含对方最近一句、当前消息、阿充语气样本和企业黑话解释。
- [x] 历史读取失败时闭合失败，不脱离上下文盲回。
- [x] 群聊、单聊、自聊、重复事件、人工接管、重试上限均有自动化合同。

## 1A 需求链路

- [x] 收到需求前先识别产品：WebAgent 或 AI 协同空间。
- [x] WebAgent 对应 `WebAgent需求池`，AI 协同空间对应 `AI采购协同空间`。
- [x] 在写需求前先读取对应代码仓，定位影响范围和待澄清点。
- [x] 需求正文必须包含背景、目标、需求清单、详细描述、验收标准、影响与风险，不建立一句话需求。
- [x] 建立后直接返回 1A 链接；后续澄清直接追加更新；状态变化通知提需求人。
- [x] 本轮仅进行两个需求池和代码仓的只读验证，没有创建测试垃圾需求。

## 运行与安全

- [x] 主服务与 Dashboard LaunchAgent 均为 running。
- [x] 审计数据库 `quick_check=ok`，备份可读且无外部写残留。
- [x] 消息去重、持久队列、失败重试、死信与防循环机制通过验收。
- [x] Dashboard 真实显示钉钉主通道、飞书已关闭、Codex CLI 已选中。
- [x] 全量自动化测试通过，机制验收 `87/87`。
